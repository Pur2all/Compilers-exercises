/*
S -> Program EOF
-> Stmt A EOF
-> Stmt ;Stmt A EOF
-> Stmt ;Stmt ;Stmt A EOF
-> Stmt ;Stmt ;Stmt ;Stmt A EOF
-> Stmt ;Stmt ;Stmt ;Stmt A EOF
-> Stmt ;Stmt ;Stmt ;Stmt EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN Expr EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN T L EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN T L EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN T RELOP T L EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN T RELOP T EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN T RELOP NUM EOF
-> Stmt ;Stmt ;Stmt ;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE Stmt;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO Stmt WHILE Expr;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO Stmt WHILE T L;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO Stmt WHILE T RELOP T L;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO Stmt WHILE T RELOP T;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO Stmt WHILE T RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO Stmt WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO ID ASSIGN Expr WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO ID ASSIGN T L WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO ID ASSIGN T WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN Stmt ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN ID ASSIGN Expr ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN ID ASSIGN T L ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN ID ASSIGN T ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF Expr THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF T L THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF T RELOP T L THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;Stmt ;IF T RELOP T THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;ID ASSIGN Expr ;IF T RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;ID ASSIGN T L ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;ID ASSIGN T ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;ID ASSIGN NUM ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> Stmt ;ID ASSIGN NUM ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> ID ASSIGN Expr ;ID ASSIGN NUM ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> ID ASSIGN T L ;ID ASSIGN NUM ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> ID ASSIGN T ;ID ASSIGN NUM ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF
-> ID ASSIGN NUM ;ID ASSIGN NUM ;IF ID RELOP ID THEN ID ASSIGN NUM ELSE DO ID ASSIGN ID WHILE ID RELOP NUM;ID ASSIGN ID RELOP NUM EOF

CORRETTO
*/

x <-- 69.69;
y <-- 69;
if x != y then
    z <-- 69.69
else
    do
        x <-- z
    while x>69;
    y <-- z < 10

